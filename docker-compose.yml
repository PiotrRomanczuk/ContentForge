services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=postgres;Database=contentforge;Username=postgres;Password=postgres
      - Auth__ApiKey=${CONTENTFORGE_API_KEY:-dev-api-key-change-me}
      - Auth__JwtSecret=${CONTENTFORGE_JWT_SECRET:-dev-jwt-secret-must-be-at-least-32-chars}
      - Auth__Issuer=ContentForge
      - Auth__Audience=ContentForge-API
      - Auth__TokenExpirationHours=24
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=contentforge
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
